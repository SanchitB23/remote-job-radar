# =========================================
# Remote Job Radar Web - Environment Configuration
# =========================================
# Next.js frontend application with React, TypeScript, and Tailwind CSS
# 
# Copy this file to .env.local (for development) and fill in your values.

# =========================================
# Clerk Authentication (Required)
# =========================================
# Get these values from your Clerk dashboard at https://clerk.com
# 1. Create a new application or use an existing one
# 2. Go to "API Keys" section
# 3. Copy the publishable and secret keys
# 4. Set up JWT template named "remote-job-radar" in "JWT Templates"

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
CLERK_SECRET_KEY=your_clerk_secret_key_here
CLERK_JWT_KID=your_jwt_kid_here
CLERK_JWT_ISSUER=your_jwt_issuer_here

# Example values (replace with your actual values):
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_abc123...
# CLERK_SECRET_KEY=sk_test_def456...
# CLERK_JWT_KID=ins_2abc123def456
# CLERK_JWT_ISSUER=https://your-instance.clerk.accounts.dev

# =========================================
# Optional: Clerk redirect URLs
# =========================================
# Customize the sign-in/sign-up flow (uncomment to use)
# NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
# NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
# NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/jobs
# NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/jobs 

# =========================================
# GraphQL API Endpoints (Required)
# =========================================
# URLs to your GraphQL API server

# Development (default):
NEXT_PUBLIC_GRAPHQL_BASE_URL=http://localhost:4000/graphql
NEXT_PUBLIC_GRAPHQL_WS_ENDPOINT=ws://localhost:4000/graphql

# Production (example):
# NEXT_PUBLIC_GRAPHQL_BASE_URL=https://api.yourdomain.com/graphql
# NEXT_PUBLIC_GRAPHQL_WS_ENDPOINT=wss://api.yourdomain.com/graphql

# Staging (example):
# NEXT_PUBLIC_GRAPHQL_BASE_URL=https://staging-api.yourdomain.com/graphql
# NEXT_PUBLIC_GRAPHQL_WS_ENDPOINT=wss://staging-api.yourdomain.com/graphql

# =========================================
# Health Check Endpoints (Optional)
# =========================================
# Used by the web app's health check API routes
# These default to the development URLs if not specified


# Job aggregator health check  
AGGREGATOR_SERVER_BASE_URL=http://localhost:8080

# ML embedder health check
EMBEDDER_SERVER_BASE_URL=http://localhost:8000

# Cron/scheduler health check (points to API server)
CRON_SERVER_BASE_URL=http://localhost:4000

# =========================================
# Next.js Configuration (Optional)
# =========================================
# Enable Next.js speed insights (if you have Vercel Pro)
# NEXT_PUBLIC_VERCEL_ANALYTICS=true

# Enable detailed build output
# ANALYZE=true

# =========================================
# Development Configuration (Optional)
# =========================================
# Port for the Next.js dev server (default: 3000)
# PORT=3000

# Enable TypeScript strict mode
# TYPESCRIPT_STRICT=true

# Enable detailed bundle analysis
# BUNDLE_ANALYZE=true
